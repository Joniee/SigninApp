<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SigninApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SigninApp" parent="Base.Theme.SigninApp" />

    <style name="TemaCalendarioSeguro" parent="ThemeOverlay.MaterialComponents.MaterialCalendar.Fullscreen">
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowFullscreen">false</item>

        <item name="colorPrimary">#4C58AF</item>

        <item name="colorOnPrimary">@android:color/white</item>

        <item name="android:windowBackground">@android:color/white</item>
        <item name="colorSurface">@android:color/white</item>

        <item name="colorOnSurface">@android:color/black</item>

        <item name="buttonBarPositiveButtonStyle">@style/BotonesCalendario</item>
        <item name="buttonBarNegativeButtonStyle">@style/BotonesCalendario</item>
    </style>

    <style name="BotonesCalendario" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">#4C58AF</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>